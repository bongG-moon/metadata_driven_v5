#가상환경 활성화
python -m venv .venv
.\.venv\Scripts\Activate.ps1

python -m pip install --upgrade pip
python -m pip install -r requirements.txt

# 로컬 실행: 사번 2011111 / 이름 문봉건으로 고정됩니다.
python -m uvicorn app_local:application --host 127.0.0.1 --port 8002

# 운영 실행: HCP 환경에서만 사용합니다.
# 기본 production 모드는 브라우저의 LASTUSER Cookie를 사용합니다.
# MONGODB_URI, MONGODB_DATABASE, PTMORE_EMPLOYEE_DIRECTORY_COLLECTION을 설정합니다.
python app.py
