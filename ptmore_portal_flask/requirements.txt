Flask>=3.0,<4.0
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
pymongo>=4.10,<5.0
pandas>=2.2,<3.0
requests>=2.31,<3.0

# portal_core.py is a compatibility copy of the existing tested Portal logic.
# Flask is the actual HTTP server; these remain only while the business layer
# is incrementally separated from its old ASGI implementation.
fastapi>=0.115,<1.0
